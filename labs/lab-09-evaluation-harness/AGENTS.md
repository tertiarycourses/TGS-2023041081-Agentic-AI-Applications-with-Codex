# Lab-local Codex instructions

- Scope all edits to this Lab 09 folder or a temporary copy inside it.
- Inspect before editing and state which files will change.
- Preserve supplied tests and expected evidence; never edit them to manufacture a pass.
- Use Python's standard library only unless this lab explicitly supplies another dependency.
- Finish only after focused tests and `python3 scripts/verify_lab.py` pass.
- Do not use credentials, personal data, network publication or destructive commands.
